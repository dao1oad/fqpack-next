[build-system]
requires = [
    "setuptools>=61.0",
    "wheel",
    "Cython"
]
build-backend = "setuptools.build_meta"

[project]
name = "fqchan01"
version = "2025.7.29"
requires-python = ">=3.12,<3.13"

[tool.setuptools]
packages = ["demo"]
