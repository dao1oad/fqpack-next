[build-system]
requires = [
    "setuptools>=61.0",
    "Cython>=3.0,<4.0"
]
build-backend = "setuptools.build_meta"

[project]
name = "fqchan04"
version = "2026.2.15"
requires-python = ">=3.12,<3.13"

[tool.setuptools]
packages = ["demo"]
