[build-system]
requires = [
    "setuptools",
    "wheel",
    "Cython",
    "pybind11"
]
build-backend = "setuptools.build_meta"

[project]
name = "fqcopilot"
version = "2026.2.17"
dynamic = ["dependencies"]
requires-python = ">=3.12,<3.13"
