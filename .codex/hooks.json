{
  "description": "Project lifecycle hooks.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$(git rev-parse --show-toplevel)/tools/governance.py\" hook-stop",
            "commandWindows": "powershell.exe -NoLogo -NoProfile -NonInteractive -Command \"$r=(& git rev-parse --show-toplevel).Trim(); $p=Join-Path $r 'tools/governance.py'; if(Get-Command py -ErrorAction SilentlyContinue){& py -3 -X utf8 $p hook-stop}else{& python -X utf8 $p hook-stop}\"",
            "timeout": 120,
            "statusMessage": "Checking governance evidence"
          }
        ]
      }
    ]
  }
}
