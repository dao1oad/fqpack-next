# CLX 重点候选（模型×触发形态）深研（2026-07-25）

事后研究，只读 sealed artifacts（run `01KBYC7REC0V3RY99634853AAB`），未预注册、未用 HOLDOUT、不构成实盘建议。承接 `2026-07-25-clx-trigger-split-vs-index.md`。

## 入选标准与名单

从 90 个（模型×触发）分组中选出 11 个：每笔均值高、n≥500（S0011×MACD_CROSS 为 586）、VALIDATION 段均值为正、剔除弱模型（S0012/S0003/S0007）。

ENGULFING：S0011 / S0006 / S0016 / S0009 / S0000；MACD_CROSS：S0013 / S0011 / S0000 / S0006；STRONG_FRACTAL：S0016 / S0006。

## 深研维度（脚本 `cand_deep.py`，容器内运行，输出 /tmp/cand_deep.json）

交易规则同触发拆分回测（T+1 QFQ 开盘入场、任一模型卖出信号 T+1 QFQ 开盘退出、扣 0.4%）。共 59,952 笔。新增维度：
- TRAIN(2005-19) vs VALIDATION(2020-23) 对比 + 中位数/均持仓/P5-P95；
- 逐年每笔均值与胜率；
- 入场价（未复权）分层（<3/3-5/5-10/10-20/20-50/>50 元，n≥30）；
- 回踩次数（occurrence）分层。

## 关键发现

1. **VAL 不衰减组（最值得深入）**：S0006×吞没（VAL +2.46%/笔、胜率 62.5%，高于 TRAIN）、S0016×吞没（VAL +2.20%、胜率 59.4%）、S0006×强分型（VAL +1.55%）、S0016×强分型（VAL +1.31%）、S0000×吞没（VAL +1.26%，n=10,282 样本大）。
2. **VAL 明显衰减组（警惕过拟合/牛市依赖）**：S0011×吞没（全部 +2.21% → VAL +0.66%；逐年看主要靠 2006 单年 +108%）、S0013×MACD（+1.66% → +1.01%）、S0000×MACD（+1.34% → +0.73%）。
3. **低价股放大效应在候选内依旧显著**（如 S0011×吞没 <3 元分层 +33%/笔），实盘容量与冲击成本是主要约束；5-20 元分层普遍仍为正，策略并非只靠仙股。
4. **首次触发规律**在多数候选上成立（第 1 次触发均值最高），与此前「首次触发放大器」结论一致。

## 可视化

`http://127.0.0.1:18099/` 新增标签页「⑫ 重点候选深研」（数据 `cand.js`，常量 CAND）：
① 候选总览（TRAIN vs VAL 分组柱图 + 悬浮完整统计）；② 净值曲线 vs 上证指数（11 条曲线复用 ⑪ 页月度净值，对数/线性/相对指数）；③ 逐年表现；④ 股价分层；⑤ 回踩次数。
